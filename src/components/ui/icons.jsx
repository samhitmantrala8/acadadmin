// Icons.jsx (optional, if you want to create a custom wrapper for icons)
import React from "react";
import { Check, X, Download } from "lucide-react";

export const IconCheck = (props) => <Check {...props} />;
export const IconX = (props) => <X {...props} />;
export const IconDownload = (props) => <Download {...props} />;
